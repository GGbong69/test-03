# 기획서 ↔ 게임 대조표

기획서 `6bbad8c9-…pptx` 36장 · 게임 `D:/workspace/test-03` @ `f82afad`

표시
- `[ ]` 아직 안 봤다
- `[o]` 게임이 기획서대로다
- `[x]` 다르다 — 아래 「다른 점」에 적는다
- `[~]` 반만 맞다
- `[-]` 대조할 것이 아니다 (일정·확장성 등)

---

## s3 게임 개요

- [o] 장르 — 스코어어택 로그라이트
- [x] 플랫폼 — PC / 모바일 → **모바일이 없다**
- [~] 조작 — PC 좌클릭 `o` / 모바일 터치 `x`

## s4 게임 진행 (흐름도)

- [o] 로비 → 다트통 선택 → 난이도 선택 → 런 시작 (`S.TITLE` → `S.NEWRUN`, 다트통과 리그를 한 화면에서 고른다)
- [x] 진행중인 런 이어하기 → **없다.** 제목 메뉴는 새 시작/도감/설정/끝내기 넷뿐이고 런을 이어 여는 길이 없다
- [o] 판 선택 → 다트 던지기 → 목표 도달? → 상점 (`S.LEG` → `S.AIM_*`/`S.FLY` → `S.CLEAR` → `S.SHOP`)
- [o] 남은 다트 없고 목표 미달 → 런 실패 → 로비 (`S.OVER`)
- [o] 런 클리어

## s5 런

- [o] 1런 = 8라운드 (`data/rounds.csv` 8줄 · 판 24)
- [o] 다트통은 런 시작 전 고르고 효과는 런 끝까지
- [o] 다트통마다 리그 진도가 따로 쌓이고 공유 안 된다 (`league_key` = `league:<다트통>:<리그>`)

## s6 다트통 (개요)

- [o] 런 시작 때 고르고 런 끝까지 이어진다
- [o] 다트통마다 리그 진도가 따로다
- [o] 런 중 조건 충족 → **다음 런 시작 시** 해금 (`_pack_unlock_check` 를 런 끝·새 런 화면에서만 부른다)

## s7 히든 다트통

- [o] 런 클리어가 아닌 특정 조건으로 해금 (`packs.csv` 의 `kind: hidden` — 저울 · `[?? ???]` 둘)
- [x] 예 — 개발자 모드 → **`0718` 다트통이 없다**
- [o] 예 — 다트가 물음표로 바뀌고 점수·배수 1~99 랜덤 (`score: rand` → `randi_range(1, 99)` 둘 다)
- [~] 해금 조건 예 — 기획서의 예(로비 1시간 / 특정 동전 소지 / 볼스아이만) 중 **쓰이는 건 없다.** 게임은 `bulls≥50`(저울) · `misses≥300`(`[?? ???]`)

## s8 다트통 표 (14줄)

효과와 해금을 따로 본다. `packs.csv` 전 줄 대조.

- [o] 기본 다트통 — 효과 없음 (동전 5칸 · 사탕 2칸 · 다트 6)
- [o] 여벌 — 다트 +1 (`darts_add 1`) · 남은 다트가 골드가 안 된다 (`dart_gold 0` — `pack_v` 는 **빈 칸**일 때만 기본값 1 로 떨어지므로 0 이 제대로 먹는다) · 해금 (`prereq base`)
- [~] 일당 — 효과 `o` (`interest_off 1` · `dart_gold 2`) / 해금 `x` — 기획서 「여벌 완주」, 게임 `prereq p_adv`(선금)
- [~] 선물 — **랜덤 아이템 2개가 아니라 비상금+젤리 고정** / −4골드 `o` / 해금은 기획서가 자기 자신을 가리킨다(오타), 게임 `prereq p_mgn`
- [~] 넓은 동전 슬롯 — 효과 `o` (슬롯 6 · 다트 5) / 해금 `x` — 기획서 「동전 4개 이하로 완주」, 게임 `prereq p_mag`
- [~] 선금 — 효과 `o` (+10골드 · 사탕 1칸) / 해금 `x` — 기획서 「100G 이상 소지 완주」, 게임 `prereq p_rack`
- [o] 깃털 — 효과 `o` · 해금 `o` (`best_dart_lgt ≥ 3`)
- [~] 송곳 — 관통 다트 `o` / **기획서에 없는 동전 슬롯 6칸이 더 붙어 있다** · 해금 `o`
- [~] 무쇠 — 효과 `o` / 해금 `x` — 기획서 「무거운 다트 3개 이상」, 게임 `prereq p_wage`
- [~] 자석 — 자석 다트 `o` / **기획서에 없는 목표 1.2배가 더 붙어 있다** · 해금 `x` (게임 `prereq p_iron`)
- [~] 저울 — 데칼코마니 `o` / **기획서에 없는 목표 1.5배가 더 붙어 있다** · 해금 `x` (게임 `bulls ≥ 50`)
- [~] 외줄 — 효과 `o` (다트 3 · 점수 ×1.6) / 해금은 뜻이 가깝되 **보스 판 조건이 없다** — 게임 `best_spare ≥ 5`(6발 중 1발로 넘긴 판, 판 종류를 안 가린다)
- [~] `[?? ???]` — 점수·배수 1~99 `o` / **기획서에 없는 목표 2배가 더 붙어 있다** · 해금 `x` (게임 `misses ≥ 300`)
- [x] `0718` — **게임에 없다**

## s9 리그 표 (8줄)

- [o] 순서 — 하얀 / 초록 / 파랑 / 보라 / 빨강 / 노랑 / 주황 / 검정
- [o] 리그가 오를수록 이전 효과에 **더해진다** — 표의 열이 실제로 누적된다(검정은 여섯 열이 다 켜져 있다)
- [o] 초록 — 작은 판 골드 0 (`reward_small 0`)
- [o] 파랑 — 목표점수가 더 빨리 상승 (`curve_a`, 라운드 8에서 2.00배)
- [o] 보라 — 매 판 랜덤 동전 1개 봉인 (`seal_items 1`)
- [o] 빨강 — 다트 1개 감소 (`darts_add -1`)
- [o] 노랑 — 목표점수가 더 빨리 상승 (`curve_b`, 라운드 8에서 4.00배)
- [o] 주황 — 상점 1.25배 (`shop_cost_mul 1.25`)
- [o] 검정 — 동전 유지비 1골드/장 · 5판 후 사라짐 (`rent 1` · `perish 5`)
- [x] 해금 — 기획서 「런 1·2·2·4·5·6·7회 클리어」, 게임은 **앞 리그를 한 번 깨면 다음이 열리는 사슬**(`prereq`). 기획서 쪽 파랑·보라가 둘 다 「2회」인 건 오타로 보이고, 1·2·3·4·5·6·7 이면 사슬과 같아진다

## s10 라운드와 판

- [o] 총 8라운드, 8라운드 클리어 = 1런 클리어
- [o] 라운드당 3판 — 작은 / 큰 / 보스 (`legs.csv` 3줄)
- [o] 작은 판 — 제약이 안 붙는다(제약은 `_open_stage` = 보스 전용) · `skippable 1`
- [o] 큰 판 — `skippable 1`
- [o] 보스 판 — 제약 셋을 깔고 하나 고른다 · `skippable 0` (검증기가 막는다)
- [~] 건너뛰기 보상 = 뱃지 — 사탕 `o` · 골드 `o` · **사진 `o`** (「보급」 뱃지가 사탕·사진이 같이 든 `consumables()` 전체에서 뽑는다) / **팩만 없다.** 그 밖에 기획서에 없는 갈래가 여섯 더 있다 — 동전 · 다트 · 트랙 승급 · 무료 리롤 · 테이블 확장 · 제약 선택지 · 외상

## s11 판 선택

- [o] 목표점수 미달로 다트 소진 → 런 실패, 로비로 (`S.OVER`)
- [o] 조준 — 상/하 후 좌/우, 교차점 착탄 (`S.AIM_V` → `S.AIM_H` → `S.CONFIRM`)
- [o] 아이템 보유 시 조준 방식이 달라진다 (`_aim_from_items` — 동전의 `aim` 열)
- [o] 판 클리어 보상 = 클리어 보상 + 남은 다트당 1골드 + 5골드당 1골드 (`gold_per_dart 1` · `interest_per 5`)
- [o] 판 중 아이템으로 골드 획득 시 줄이 따로 표시 (`clear_gold_detail` 에 `_gold_from_items` 를 덧붙인다)

## s12 트랙과 강화

- [o] 트랙 다섯 — 싱글 / 더블 / 트리플 / 불(아우터+이너 공유) / 보드 아웃 (기획서의 「보드 아이」는 「보드 아웃」의 오기로 보인다)
- [o] 사탕을 쓰면 고른 트랙 레벨 +1 (`_cons_use` 의 `area` 갈래)
- [x] **강화 곡선이 다르다.** 기획서 「홀수 레벨당 점수, 짝수 레벨당 배수」 / 게임 `area_upgrades.csv`:
  - 점수는 **레벨마다 매번** 오른다 (싱글 +5 · 더블 +8 · 트리플 +11 · 불 +40 · 보드 아웃 +6)
  - 배수는 **홀수 레벨에만** 오르고(더블·트리플), 불은 **매 레벨** 오르며, 싱글·보드 아웃은 **한 번도 안 오른다**

## s13 제약 표 (10줄)

- [o] 보스 판에서 3장 중 1장 고르기 (`stage_picks 3`) · 그 보스 판에만 적용 (`active_mods` 는 `_pick_stage` 에서만 갈린다)
- [o] 실띠 — `band_mul 0.50`
- [o] 역풍 — `gauge_mul 2.00`
- [o] 단벌 — `darts_add -1`
- [o] 금지 구역 — `sector_kill`
- [o] 먹통 — `seal_items 1`
- [o] 문턱 — `target_mul 1.25`
- [o] 그늘 — `color_kill`
- [o] 민짜 — `ring_kill 3`
- [o] 홀대 — `odd_mul 0.50`
- [x] 돌린 판 — 기획서 「보드칸의 **값이 랜덤**」 / 게임은 `spin 7`, 판을 일곱 칸 **회전**시킨다. 값은 하나도 안 바뀌고 자리만 옮긴다 (`qa_mods` 에서 난이도 +1.1% — 사실상 안 아픈 유일한 제약)

## s14 챌린지 표 (7줄)

`qa_chal` 14검사 전부 통과. 표의 열 열넷이 모두 코드에 닿는다.

- [o] 빈손 — 골드 0 확인 · 뱃지 골드 6 → 12 확인
- [o] 홑장 — 슬롯 5 → 1 확인 · 정산 큐에 4 → 12 확인
- [o] 깜깜이 — 안개 상시 확인 · 목표 50 → 35 확인
- [o] 큰손 — 12골드 → 0 확인 (`shop_round 1` 도 코드에 닿는다)
- [o] 목표물 — 판마다 칸을 뽑고, 그 칸만 점수가 나고 다른 칸은 0 확인
- [o] 외상 — 시작 −20골드 확인 (`interest_mul 2` 도 코드에 닿는다)
- [o] 겹치기 — 서로 다른 제약 둘 확인 (`reward_mul 2` 도 코드에 닿는다)
- [x] 챌린지를 고르는 화면 → **없다.** 개발자 모드 「챌린지」 줄로만 켠다

## s15 무한모드

- [x] 8라운드 클리어 후 「계속 하기 / 로비로」 선택 → **없다.** 판 24를 넘기면 `_finish_leg` 가 곧장 `S.OVER · won = true` 로 끝낸다
- [x] 무한모드 목표점수 함수 → **없다.** 목표 곡선은 `rounds.csv` 8줄이 전부라 라운드 9 이후를 낼 수가 없다

## s16 조준 방식 (1/2)

여덟 종이 다 있다 (`AIM_MODES` 8개, 잠금 횟수 `AIM_STAGES` 도 맞다). **이름이 여섯 개 다르다.**

- [o] 기본 = `std` — 두 번 잠금
- [o] 원 = `ring` — 두 번 잠금
- [~] 멀미 = `tilt` **「빗각」** — 동작은 맞다(조준선이 다트마다 다른 각도로 기운다), 이름이 다르다
- [o] 겹 = `cross` — 한 번 잠금 (기획서 「두 번째 조정 없음」과 맞다)

## s17 조준 방식 (2/2)

- [~] 수전증 = `drift` **「흔들」** — 동작 맞다, 이름 다르다
- [~] 명사수 = `place` **「놓기」** — 동작 맞다, 이름 다르다
- [o] 단검 = `pull` — **왼쪽에서 던지는 게 맞다.** `PULL.org` 가 `(40, 224)` 로 판 왼쪽 벽이고 `_pull_grab` 주석도 「판 위가 아니라 왼쪽 벽에서 쥔다」다. 던지는 힘은 놓는 순간의 속도로 잰다
- [~] 리볼버 = `kick` **「반동」** — 반동은 맞다. **6발이 아니라 5발**(`kick_n 5`)이고 발마다 점수의 25%만 받는다(`kick_share 0.25`) — 그 몫 규칙은 기획서에 없다

## s18 점수 계산

정산 큐가 `chip = info.base` · `mult = info.mult` 로 시작하고 그 위에 동전 수량이 더해진다 — 기획서의 「(착탄 배수 + 아이템 배수)」 그대로다.

- [o] 점수 = 위치점수 × 아이템 합
- [o] 싱글 착탄 — 싱글의 영역 배수가 1 이라 결과가 「착탄 점수 × 아이템 배수」와 같다
- [o] 더블·트리플 착탄 — 착탄 점수 × (착탄 배수 + 아이템 배수)
- [o] 보드 아웃 착탄 — 보드아웃 점수 × (보드아웃 배수 + 아이템 배수)
- [~] 예시 — 미보유 20×3 `o` · 보유 (20+1)×(3+1) `o` · **강화 몫이 다르다**: 기획서 +10, 게임은 트리플 트랙 레벨당 **+11** (`area_upgrades.csv`)

## s19 상점

- [o] 판 클리어 후에만 들어간다 (`_settle_clear` → `S.CLEAR` → `S.SHOP`)
- [~] 골드 / 다트 / 사탕 / 사진 랜덤 배정 — 게임이 까는 갈래는 **동전 · 사탕 · 사진 · 보드 확장 · 다트 · 팩** 여섯. **골드는 매물로 안 뜬다**
- [o] 판매는 왼쪽(`Z_SELL`), 구매는 오른쪽(`Z_BUY`)
- [o] 테이블에 떨어진 아이템은 판매 불가 (`_can_sell` — 손에 든 동전만 판다)
- [o] 리롤 — 판당 1회 무료(`free_rerolls 1`), 이후 2골드(`reroll_base 2`)
- [o] 유료 리롤 1회마다 값이 1씩 오른다 (`reroll_step 1`)
- [o] 동전 5칸 · 사탕 2칸 (`packs.csv` 기본 `item_slots 5` · `cons_slots 2`)
- [o] 슬롯이 가득 차면 그 갈래 아이템 구매 불가 (`_buy` 가 `max_items`/`cons_slots` 에서 막는다)
- [o] 팩 — 작은 팩 2개 중 1, 큰 팩 4개 중 1 (`boosters.csv` `size 2`/`4` · `pick 1`)

## s20 동전 (개요)

- [o] 동전은 점수증가 / 배수증가 / 배수×2 로 점수에 직접 영향 (`kind` = `chip` · `mult` · `xmult` …)
- [o] 발동 순서 — 슬롯 왼쪽부터 오른쪽 (`for i in owned.size()` 순으로 큐에 쌓인다)
- [o] 순서를 플레이어가 바꿀 수 있다 (`_can_rack_move` — 상점·판 고르기·판 중 전부)
- [o] 획득 — 상점 구매 / 뱃지(`kind: item`) / 팩(`_boost_one`)
- [o] 등장 확률 — 가중치 1.00 / 0.58 / 0.08 / 0.00 → **60.2% · 34.9% · 4.8% · 0%**, 기획서의 60/35/5/0 과 맞는다

## s21 레전더리 동전

- [o] 팩에서 확률적으로 등장하거나 특정 조건으로 획득 (등급 가중치 0 이라 상점 테이블에는 안 뜬다)
- [o] 조건을 처음 충족하면 **바로 다음 상점에서 무료로** (`_item_free_pick` → `_roll_stock` 의 `free: true` 한 장)
- [o] 런이 끝난 뒤 다음 런부터는 팩에서 확률적으로 (`_boost_one` 이 `itemgot:` 표식이 있는 전설만 `legend_pack_w` 로 넣는다)
- [o] 예 — 잭과 콩나무 = `side: bigdart`
- [~] 예 — NULL = `side: boardkill`, 즉시 클리어 `o` / **`?` 다트통 해금과 연결돼 있지 않다** (`p_rnd` 는 `misses ≥ 300`)
- [o] 예 — 정조준 = `aim: place`
- [x] 해금 조건이 **임시값**이다. 코드 주석이 그렇게 적어 두었다 — 정조준 `bulls≥150`(기획서는 다름) · NULL `best_score≥100000`(기획서는 「한 번의 투척으로 1000000점」) · 잭과 콩나무 `best_leg_bare≥24`
- [x] **데칼코마니는 얻는 길이 저울 다트통뿐이다.** 해금 조건이 없고 가중치도 0 이라 팩에도 안 뜨는데, 기획서 s8 은 저울의 해금을 「데칼코마니 해금」이라 적어 서로를 가리킨다

## s22~27 동전 목록 (99장)

- [o] 이름 99개가 게임과 1:1로 맞는다
- [o] 가격이 전부 맞는다 (일반 4G · 희귀 8G · 레어 12G · 전설 20G)
- [o] 희귀도가 전부 맞는다 (일반 50 · 희귀 30 · 레어 15 · 전설 4)
**99장 중 66장만 켜져 있다.** 꺼진 33장은 엔진이 아직 못 하는 것들이라 표에는 있어도 게임에 안 뜬다 (`enabled 0`).

- [~] s22 (1/6) — 19장 중 18장 켜짐. 꺼짐: 루나 코인(판마다 1/10 파괴 — 확률 표에 1/6·1/1000 뿐)
- [~] s23 (2/6) — 19장 중 10장 켜짐. 꺼짐 9: 찰리의 초콜릿 · 8번 입구 · 단골 손님 · 와인 · 사탕포장지 · 빛나는 동전 · 잡동사니 · 엣지러너 · 숙제
- [~] s24 (3/6) — 19장 중 13장 켜짐. 꺼짐 6: 시잔기 · 책문관의 바퀴 · 소갈 · 의사 콜라 · π · 다시! 또 다시!
- [~] s25 (4/6) — 19장 중 **9장만** 켜짐. 꺼짐 10: 장기 투자 · 난쟁이의 축구공 · 사직서 · 녹슨 동전 · 대머리 · D20 · 오크통 · 도플겡어 · To The Mars · 0+0
- [~] s26 (5/6) — 19장 중 12장 켜짐. 꺼짐 7: 시계 · 시아노타입 · COPPEE · 그레고르 잠자 · 보팔 소드 · 자판기 · 조개 껍질
- [o] s27 (6/6) — 전설 4장 전부 켜짐

켜진 66장은 조건(`cond`)과 갈래(`kind`)가 기획서 문구와 맞는다. 수치는 밸런스라 안 셌다. 다만 셋은 **문구 자체가 어긋난다**:

- [x] 빌리의 바지 — 기획서 「점수 +40 · **배수 +4**」 / 게임 `v2 = 40` (배수 +40). 자릿수 오타로 보인다
- [x] 조준 동전 셋 — 더 사일런트 · 카우보이 · 석양이 진다에 적힌 「점수의 배수의 배율이 증가/감소한다」가 **게임에 없다**. 조준 방식만 바꾸고 끝난다
- [x] 조준 동전 이름 — 교통카드「멀미」→ `tilt`(빗각) · 수전증「수전증」→ `drift`(흔들) · 더 사일런트「단검」→ `pull`(당김) · 석양이 진다「리볼버」→ `kick`(반동). s16·s17 의 이름 차이가 동전 설명에도 그대로 드러난다

## s28 보드 확장 (개요)

- [o] 보드의 특정 트랙을 넓히거나 줄이거나 더한다 (`_mod_step` 축 12종)
- [~] 획득 — 상점 구매 `o` / **건너뛰기 보상으로는 못 얻는다** (`tags.csv` 에 보드 확장 갈래가 없다)

## s29 보드 확장 표 (12줄)

값은 12줄 전부 12G `o`. 효과는 `qa_board` 로 실측했다(괄호 안은 기본 판 대비 판값).

- [~] 핵심 — 트리플이 7.61% → 12.17% 로 넓어진다 `o` / **기획서에 없는 대가로 더블이 11.84% → 4.89% 로 줄어든다** (`band` 축은 링 총 폭 0.20 을 보존해야 해서 검증기가 막는다) · (+2.4%)
- [o] 테두리 — 트리플 → 3.04% · 더블 → 18.35% (−2.9%)
- [o] 천체 고리 — 트리플 7.61% → 11.79%, 더블은 그대로 (+9.1%)
- [o] 대기권 — 아우터 불만 넓어지고 트리플·더블은 그대로 (+1.9%)
- [o] 라지 — 빗나감 37.66% → 24.57% (+28.6%)
- [o] 역지사지 — `swap 3` (+24.7%)
- [o] 홀 — 짝수 칸 −1 → 스무 칸이 다 홀수 (−4.6%)
- [o] 짝 — 홀수 칸 +1 → 스무 칸이 다 짝수 (+4.6%)
- [o] 시계 — `flat 12` (+13.7%)
- [o] 과녁 — 칸 값 10 · 불 배수 5, 트리플 3 · 더블 2 · 싱글 1 은 `areas.csv` 가 이미 그 값 (+10.5%)
- [~] 피자 — 칸 값 16 `o` / **띠를 지우는 게 아니라 배수를 1 로 내린다.** 기하는 그대로라 트리플·더블 넓이가 안 변한다(7.61% · 11.84%) — 점수 결과는 같다 (+5.4%)
- [~] 도넛 — 칸 값 +3 `o` / **불이 사라지는 게 아니라 최소치로 줄어든다** (판의 0.1%, 조준으로는 못 닿는다) (+25.6%)

## s30 다트 표 (6줄)

- [o] 기본 — 효과 없음
- [o] 무거운 — `gauge 0.55` · `mult -1` · 5G
- [o] 가벼운 — `gauge 1.90` · `mult 3` · 6G
- [o] 관통 — `pierce_side 0.50` · `fix1 1` · 7G
- [~] 자석 — 중심으로 당김 `o` (`magnet 0.25`) · 7G `o` / **기획서에 없는 배수 −1 이 붙어 있다**
- [~] `?` — 점수·배수 1~99 는 다트통이 쥔다(`score: rand`). **다트 표에는 `?` 줄이 없다** — 다트가 아니라 계산 방식으로 넣었다

## s31 사탕 표 (5줄)

효과는 다섯 다 맞다. **값이 셋 다르다** — 기획서는 전부 4G.

- [o] 젤리 — 싱글 · 4G
- [x] 사탕 — 더블 · **6G** (기획서 4G)
- [x] 지팡이 사탕 — 트리플 · **6G** (기획서 4G)
- [x] 막대 사탕 — 불 · **9G** (기획서 4G)
- [o] 초콜릿 — 보드 아웃 · 4G

## s32 사진 표 (8줄)

여덟 장 전부 효과·값이 맞는다. `qa_photo` 26검사 통과.

- [o] 비상금 — `gold` · `v 20` · 15G
- [o] 동전 줍기 — `sellsum` · 15G
- [o] 불나방 — `burn` · `v 2` · 15G
- [o] 면죄부 — `pardon` · 15G
- [o] 재도전 — `redo` · 15G
- [o] 위조화폐 — `clone` · 30G
- [o] 페인트 — `paint` · `v 2` · 15G
- [o] 한 수 앞 — `peek` · 15G

## s33~35

- [-] s33 게임 화면 — 시안
- [-] s34 개발일정
- [-] s35 확장성 (랭킹 / 멀티플레이) — 범위 밖

---

## 다른 점 — 요약

기획서 36장을 다 훑었다. 무게순으로 놓으면 이렇다.

### 통째로 없는 것
| 어디 | 무엇 |
|---|---|
| s15 | **무한모드** — 판 24를 넘기면 그대로 런이 끝난다. 「계속 하기 / 로비로」도, 라운드 9 이후 목표 곡선도 없다 |
| s4 | **진행중인 런 이어하기** |
| s14 | **챌린지 고르는 화면** — 표와 효과는 다 있는데 개발자 모드로만 켠다 |
| s8 | **`0718` 다트통** |
| s3 | **모바일·터치** |

### 규칙이 다른 것
| 어디 | 기획서 | 게임 |
|---|---|---|
| s12 | 홀수 레벨당 점수 · 짝수 레벨당 배수 | 점수는 매 레벨 · 배수는 홀수 레벨(더블·트리플)/매 레벨(불)/없음(싱글·보드 아웃) |
| s13 | 돌린 판 — 칸 값이 랜덤 | 판을 7칸 회전(값은 그대로) |
| s17 | 리볼버 1발 → 6발 | `kick` 5발 · 발마다 점수의 25% |
| s10 | 건너뛰기 보상 = 사탕/사진/팩/골드 | **팩만** 없다. 대신 동전·다트·트랙·리롤·테이블·선택지·외상 일곱이 더 있다 (기획서로 리스트 받기로 함) |
| s19 | 상점에 골드가 배정된다 | 골드는 매물로 안 뜬다 |
| s9 | 리그 해금 = 런 N회 클리어 | 앞 리그를 한 번 깨면 다음이 열리는 사슬 (기획서 파랑·보라가 둘 다 「2회」인 건 오타로 보인다) |
| s8 | 다트통 해금 여섯 (동전 4개 이하 완주 · 100G 소지 · 무거운/자석 다트 3개 · 데칼코마니 · NULL 소지) | 전부 `prereq` 사슬이나 다른 통계로 대체 |
| s21 | 전설 해금 조건 | 셋 다 임시값 (코드 주석이 그렇게 적어 두었다) |

### 반쪽만 들어간 것
| 어디 | 무엇 |
|---|---|
| s8 | **선물 다트통** — 랜덤 아이템 2개가 아니라 비상금+젤리 고정 |
| s26 | **조준 동전 셋** — 「점수의 배수의 배율이 증가/감소」가 없다 |
| s29 | **핵심** — 트리플을 넓히는 대신 기획서에 없는 더블 축소가 따라온다 |
| s29 | **피자** — 띠를 지우는 게 아니라 배수를 1 로 내린다 (결과는 같다) |
| s29 | **도넛** — 불이 사라지는 게 아니라 최소치로 줄어든다 |
| s28 | 보드 확장을 건너뛰기 보상으로 못 얻는다 |

### 기획서에 없는데 게임에 붙은 것
- 송곳 다트통 — 동전 슬롯 6칸
- 자석 다트통 — 목표 1.2배 / 저울 — 목표 1.5배 / `[?? ???]` — 목표 2배
- 자석 다트 — 배수 −1

### 수치만 다른 것 (밸런스라 안 건드렸다)
- 사탕 값 — 사탕 6G · 지팡이 사탕 6G · 막대 사탕 9G (기획서는 다 4G)
- s18 예시의 트랙 강화 몫 — 기획서 +10, 게임 +11
- 동전 99장의 효과 수치 대부분

### 데이터 오타로 보이는 것
- **빌리의 바지** — 기획서 「배수 +4」, `items.csv` 는 `v2 = 40`

### 아직 못 만든 동전 33장
표에는 99장이 다 있지만 **켜진 것은 66장**이다. 나머지 33장은 엔진이 아직 못 하는 것들이다 — 아이템 생성 · 재발동 · 복사 · 상점 훅 · 판매가 성장 · 판매 훅 · 규칙 변경.
