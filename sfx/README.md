# 소리 파일

여기 있는 `.wav` 67개는 **손으로 만든 것이 아니라 구운 것이다.** 원본은 파일이
아니라 수치고, 그 수치는 `scripts/tools/sfx_bake.gd` 의 `_build` 에 있다.

```
godot --headless --script scripts/tools/sfx_bake.gd     # 굽는다
godot --headless --import                               # .import 를 세운다
godot --headless --script scripts/tools/probe_sfx.gd    # 잰다
```

파일을 직접 고치지 마라 — 다시 구우면 지워진다. 소리를 바꾸려면 `_build` 의
그 이름을 고친다. 잡음은 씨를 박은 LCG 로 내므로 몇 번 구워도 같은 파일이
나온다.

## 무엇이 무엇에 닿는 소리인가

**판 위는 다트, 판 밖은 카지노다.** 다트가 코르크에 박히는 소리는 다트여야
하므로 던지기와 착탄은 물리 그대로 두고, 그 바깥에서 칩·동전·릴·종이 난다.
저장소가 이미 「판 밖과 판 위」로 갈라 생각하던 그 선을 그대로 쓴다.

일곱 가족이 각자 **한 물체**를 쓴다. 등급과 짝은 그 물체 위에서 감쇠 · 부분음
개수 · 반음 차로만 갈린다 — 자리마다 다른 악기를 쓰면 게임이 한 목소리를 잃는다.

| 가족 | 물체 | 비율 |
|---|---|---|
| 던지기 | 코르크 판에 강철 다트 | 1 : 2.76 : 5.40 : 8.93 |
| 착탄 | 98Hz 판 모드 위의 결 + 접점 | 위와 같은 판, 쇠줄만 1 : 1.509 |
| 정산 | 코르크에 박힌 놋쇠 혀 + 동전 접시 | 혀는 위와 같음, 동전 1 : 1.59 : 2.13 : 2.30 : 2.65 |
| 판과 런 | **잭팟 종**(금고쪽) · 카드(카드쪽) | 종 hum 0.5 : prime 1 : tierce 1.2 : quint 1.5 : nominal 2 |
| 상점 | **현금 서랍과 동전 트레이** | 점토 칩 1 : 1.73 : 2.33, 원판 1 : 2.08 : 3.41 : 5.06, 접시 1 : 2.41 : 3.83 |
| 손 | **점토 칩** | 1 : 1.73 : 2.33 |
| 메뉴 | **슬롯 릴 디텐트**와 레버 | 점토 칩 1 : 1.73 : 2.33, 원판 1 : 2.08 : 3.41 : 5.06, 판스프링 1 : 1.61 |

## 동전이 부서진다 (2026-09-19)

판 끝 파괴는 **판 밖**이다 — 정산 화면의 어휘가 이미 릴 굴림과 칩이라
(`_draw_clear` 머리말) 카지노 가구 위에서 낸다. 넷 다 뼈대가 `shop_smash`
다섯 층 그대로고 **재질층은 ②③④, 공통층은 ①⑤** 라 가운데만 갈린다.

| 이름 | 무엇이 무엇에 닿는가 | 길이 | 갈리는 자리 |
|---|---|---|---|
| `coin_break` | 동전 하나가 트레이 턱에 부서진다 | 150ms | `shop_smash` 를 동전 크기로 — 파열 둘 · 꼬리 다섯 |
| `coin_break_glass` | 유리 동전이 깨진다 | 130ms | 파열 **넷** · 비조화 몸통 · 꼬리 아홉 · **밑이 없다** |
| `coin_break_wax` | 밀랍 동전이 녹는다 | 260ms | **파열 층이 없다** · 하강 글라이드 · `_lp 2600` |
| `coin_safe` | 굴렸는데 안 부서졌다 | 70ms | 파열도 밑도 없다 — **부서지는 층의 부재**가 내용이다 |

주물(`cast`)과 빈 것(`hollow`)은 **파일을 안 굽는다.** `coin_break` 를
×0.84 · ×1.18 로 민다 — `_sfx` 의 `pitch_scale = f/SFX_BASE` 가 그 길이다.
**여섯 재질, 네 파일.**

⚠ 유리에 **9.7kHz 딸깍 대역을 열지 않았다.** 반려당한 자리라 비워 두고
`_lp 6000` 을 그대로 쓴다 — 유리는 대역이 아니라 **밀도 · 비조화 비 ·
빠른 감쇠**로 낸다. 부분음이 촘촘하고 비조화이고 전부 빨리 꺼지면 6kHz
아래에서도 유리다. `qa_wreck` 이 넷의 상한 위 에너지를 재서 못 박는다.

## 등급이 나타난다 (2026-09-20)

매물이 레일에서 떨어져 펠트에 **처음 앉는** 순간이다. 판 밖이라 상점 가족
위에서 낸다 — 현금 서랍(`shop_open`)과 동전 트레이가 이미 선 그 카운터다.

| 이름 | 무엇이 무엇에 닿는가 | 길이 | 밑음 |
|---|---|---|---|
| `coin_land` | 얇은 금속 원판이 펠트에 앉는다 | 110ms | **392** |
| `coin_plaque` | 두꺼운 판재가 트레이에 앉는다 | 240ms | **262** |

`coin_break` 가 「여섯 재질, 네 파일」이라면 이쪽은 **네 등급, 한 파일**이다.
일반은 **안 낸다**(침묵이 일반의 표식이다 — `_glow_of("common").a == 0` 규약의
소리 쪽 짝), 희귀 392, 레어 523, 레전더리만 `coin_plaque` 262. 레전더리
예고의 톡 셋(294 · 330 · 392)도 같은 `coin_land` 한 파일이다.

⚠ **밑음 392 규약.** `_sfx` 의 `pitch_scale = f/SFX_BASE` 는 파일의 밑음이
아니라 **SFX_BASE(392) 대비**다. 그래서 `coin_land` 는 반드시 392 로 굽는다 —
그래야 `_sfx("coin_land", 523.0)` 이 정확히 523 으로 난다. `coin_plaque` 는
262 로 굽고 **인자 없이** 부른다(`_sfx("coin_plaque")` → 피치 1.0); f 를 주면
262/392 = 175Hz 로 난다. 이 계약이 여기 남아야 다음 사람이 파일을 더 안 굽는다.

⚠ **잭팟 종을 안 빌렸다.** 종은 `run_win` · `target_hit` · `hit_bull_i` 가
「이겼다」로 쥐고 있고 상점에서는 한 번도 안 운다. 등장에 종을 쓰면 「나왔다」와
「이겼다」가 같은 말이 되고, 팩을 뜯고 곧장 정산으로 가는 흐름에서 둘이 붙어
난다. 그래서 위 둘에 **tierce 1.2 를 한 개도 안 넣었다** — 종을 종으로 만드는
것이 그 비다. `coin_plaque` 만 판재 비(1 : 2.76 : 5.40 : 8.93)를 쓰는데,
레전더리는 원반이 아니라 **플라크**라 알맹이부터 종이 다르기 때문이다.

⚠ `coin_plaque` 는 240ms 라 `sfx_pool` 네 자리 중 하나를 오래 문다.
**판 하나에 한 번만 나는 소리**라 「0.3초 넘는 소리는 한 번만 나는 자리에만」
규약 안이고, 게임 쪽 `LAND.hush` 0.30 과 `LAND.max` 4 가 그것을 구조로 막는다.

착탄에는 등급 여섯 **밑에 같이 깔리는** 한 층이 더 있다(`board_thud`).
등급은 점수를 적고 그것은 물체를 적는다 — 어디에 꽂히든 판이 내는 몸소리는
하나여야 해서, 같은 판의 몸통(131 = 392/3)과 걸이(98Hz)만으로 짧게 낸다.
부르는 쪽이 ±6% 안에서 음을 밀어 세 발이 「툭 툭 툭」 으로 갈린다.

같은 판이 **깨지는** 소리가 하나 더 있다(`board_break`). 목표를 넘긴 판이
금을 따라 갈라져 뜨는 그 순간이고, `board_thud` · `egg_crack` 과 한 집이다 —
유리가 아니라 나무와 철사다. `egg_crack`(제목 판)과 갈리는 것은 **끊기는
철사 한 겹**(1 : 1.509, `_hit_c` 의 그 비)과 더 내린 천장(4.6kHz)이다.
두 판이 같은 소리로 깨지면 둘이 같은 물건이 된다.
음이 **내려간다** — 작은 ×1.06 · 큰 ×0.97 · 보스 ×0.88. 이 파일의 사다리는
전부 오름인데(`leg_clear` · `target_hit`) 등장은 올리고 퇴장은 내린다.
비가 1.095 · 1.101 로 서로 달라 한 음정이 소리를 규정하지 않고, tierce 1.2 는
어디에도 안 놓는다.

그 한 방 **앞에** 금이 번지는 0.68초가 있고, 거기에 여린 삐걱이 한 단에 한 알씩
깔린다(`board_crack`, 2026-09-24). 앞서는 그 0.68초에 소리가 한 알도 없었고,
층 표의 꼬리 톡이 작은 판에서 0회라 **런에서 맨 처음 듣는 깨짐이 0.30초짜리 한
방**이었다. `board_break` 의 삐걱 한 줄(700/350)을 36ms 로 잘라 여리게 뽑은
것이라 같은 기구에서 나오고, 끊기는 철사는 **안 섞는다** — 철사가 끊기는 것은
판이 실제로 갈라지는 그 한 번뿐이라 여기 섞으면 발화의 신호가 닳는다.
음은 한 깨짐 안에서 ×1.05 → ×0.92 로 내려간다(금이 바깥으로 갈수록 무는 나무가
크다). 빨리 보기에서는 꼬리 톡과 같은 문으로 안 낸다.

98Hz 는 `SFX_BASE`(392)의 1/4 이고, 손의 `rack_move` 는 392 그 값이다 —
정산 사다리와 음이 맞아야 한다.

종을 종으로 만드는 것은 **tierce 1.2(단3도)**다. 그것 없이 부분음만 쌓으면
바(bar)가 되고, 바는 이기는 자리에 안 맞는다. 이기는 쪽이 종이면 지는 쪽은
**막힌 종**이다 — `run_lose` 는 tierce 와 hum 만 남기고 곧 죽는다.

칩의 알맹이는 표의 `f` 를 **2.5배 올린 자리**에 앉는다(`CHIP_UP`). 표의 f 는
관계(4도 짝 · 옥타브)이지 칩의 대역이 아니다 — 147~349 를 그대로 기음으로 쓰면
칩이 아니라 둔한 툭이 된다. 실제 점토 칩의 알맹이는 700~1200Hz 대다. 관계를
귀가 붙잡을 닻으로 표의 f 자리에 조용한 무게 하나를 남긴다.

칩의 정체는 **감쇠**다. 몸통이 4ms 대에서 죽고 마른 테두리가 2ms 다 — 이보다
길면 칩이 아니라 접시가 되고, 부분음을 더하면 쇠가 된다.

잭팟이 둘 있다. `run_win`(종 다섯 + 쏟아지는 동전 열여섯)과 `target_hit`
(종 둘 + 열두 닢). `hit_bull_i` 에만 종을 아주 얕게(0.14) 한 번 얹었다 —
쇠줄이 아니므로 「불에는 방사 쇠줄이 없다」는 등급 신호는 그대로다.

## 크기는 여기서 안 정한다

`game.gd` 의 `SFX` 표가 정한다. 굽는 자리가 그 표의 한 줄로 **합성음을 그대로
렌더해서**(`sfx_voice.gd`) 앞 50ms 에너지를 재고, 파일을 그 에너지에 맞춘다.

피크를 표의 `a` 에 맞추면 안 된다. 두 소리의 속이 다르다.

| | 단위피크 RMS |
|---|---|
| 합성음 (사인 55% + 사각 45%, 감쇠 `(1-t/d)²`) | 0.818 |
| 모달합 (사인 여럿, 감쇠 `exp(-t/tau)`) | 0.12 ~ 0.18 |

같은 피크로 맞추면 파일이 10dB 넘게 **작게** 들린다. 그래서 물려받는 것은
피크가 아니라 에너지다. 결과로 나오는 피크는 표의 `a` 의 1.2~2.8배가 된다.

**한 번 이것이 깨졌다.** 빌려 온 UI 팩은 0dB 로 정규화돼 있어서 판 밖(메뉴 ·
상점 · 진열대)이 판 위(던지기 · 착탄 · 정산)보다 피크로 평균 16.5dB, 최대
22dB 크게 울렸다. 자리마다 수치는 다 맞는데 게임 전체가 "안 맞는" 것이 그 하나
에서 왔다. `probe_sfx` 가 지금 그 값을 잰다.

봉우리가 뾰족한 자리는 정규화 **전에** 드라이브(`_drive`)를 걸어 속을 채운다.
피크는 안 올리고 에너지만 올리므로 같은 크기를 더 낮은 피크로 낸다.

## 길이

한 음이면 표의 `d` 근처, 여러 음이면 `gap*(n-1)+d` 다. 표의 박자가 곧 소리의
길이다. 자리가 넷뿐이라(`sfx_pool`) 여운 안에 다섯 번 누르면 앞엣것이 끊긴다.
0.3초를 넘는 것은 **열하나**다 — `target_hit` 0.752 · `run_win` 0.657 ·
`run_lose` 0.500 · `settle_total` 0.420 · `hit_bull_i` 0.410 · `leg_clear`
0.370 · `fixture_buy` 0.360 · `boss_seal` 0.350 · `shop_open` 0.340 ·
`settle_bal` 0.340 · `board_break` 0.320.

⚠ 이 절이 한 번 틀렸다 — 여덟만 적고 `run_lose` · `boss_seal` · `board_break`
셋을 빠뜨린 채 「전부 한 번씩만 나는 자리다」로 닫고 있었다. 앞의 열은 지금도
한 번씩만 난다. **`board_break` 만 다르다**(2026-09-24) — 한 방은 한 번뿐이지만
같은 사건이 앞뒤로 소리를 더 낸다: 금이 번지는 0.68초 동안 `board_crack` 이
단마다(최대 여섯), 판이 뜬 뒤 `board_thud` 가 층−1 번(보스 둘). 이 표에서 한
사건이 소리를 여러 알 내는 첫 자리이고, 그래서 자리 넷을 넘는지가 사람 손이
아니라 `qa_break` 의 「한 사건의 소리가 pool 4 밑」에 걸려 있다. 다음에 이
표를 늘릴 때 세어야 하는 것은 이름 수가 아니라 **한 사건이 내는 알 수**다.

## 쏟아지는 동전

`_pour` 가 같은 동전 비율을 여러 개, 자리와 음을 흩어 얹는다. 흩는 값은 씨를
박은 LCG 라 몇 번 구워도 같은 소리가 나온다 — 엔진 RNG 를 쓰면 판이 바뀔 때
소리가 달라진다. 트레이(`_tray`)가 그 아래에 깔려 낱개를 하나로 묶는다.

## 정산 사다리

`settle_step` · `settle_pierce` · `settle_item` 은 `_sfx(name, f)` 로 음을 받아
`pitch_scale = f/392` 로 **파일 전체가** 리샘플된다. 사다리 천장이 3.78배라,
5kHz 를 안 넘으려면 렌더 시점의 알맹이가 1323Hz 아래여야 한다. 그래서 이 셋만
부분음 상한이 1250Hz 고, 5.40 배 대신 3.15 배를 쓴다(1 : 2.76 : 3.15 도
비배음이라 금속이 산다). `settle_item` 의 칩 테두리도 여기서만 2600 대신
**1200Hz** 다 — 2600 을 쓰면 천장에서 9.8kHz 로 올라가 반려당한 그 유리
딸깍이 된다. `probe_sfx` 가 그 위에 남는 에너지 몫을 잰다 — 지금 1.2~2.3% 다.

## 임포트

`.import` 도 굽는 자리가 같이 쓴다. 두 값이 기본과 다르다.

- `compress/mode=0` — 기본값 QOA 는 48ms 딸깍의 첫 순간을 뭉갠다
- `edit/trim=false` — 트림 문턱이 절대 -30dBFS(0.0316)다. 이 파일들은 피크가
  0.1~0.4 라 그 문턱이 피크의 10~30% 다. 트림을 켜면 어택 램프와 여운을
  통째로 잘라 간다. 앞무음은 애초에 0 으로 굽으므로 트림이 할 일도 없다.

## 들어 보는 법

개발자 모드(`\`)의 **소리** 쪽. 이름 67개를 화살표로 짚으면 그 자리에서 나고,
정산 사다리 13칸 · 착탄 사다리 여섯 · 손 짝 넷을 줄줄이 들을 수 있다.
굽는 자리는 전부를 한 줄로 이은 `.godot/sfx_sheet.wav` 도 떨군다.
